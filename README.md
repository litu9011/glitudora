# Family Budget Dashboard (Localhost:3000)

Simple local budget dashboard for January-April 2026.

## Run

```bash
npm start
```

Then open:

- http://localhost:3000

## Why you may see "localhost refused to connect"

That error means the server process is not running yet (or has stopped). Start it again with `npm start`.
